// @ts-nocheck

import { Navigate } from "react-router-dom";
import { useAppSelector } from "../hooks/redux";

const Reservations = () => {
  return <div className="container">RESERVATIONs</div>;
};
export default Reservations;
